import React from 'react'

interface flashCardProps{
    // id:number,
    // question:string,
    // answer:string,
    // options:any
    SAMPLE_FLASHCARD:[]
}


function FlashcardList({SAMPLE_FLASHCARD}:flashCardProps) {
  return (
    <div>
        FlashcardList
    </div>
  )
}

export default FlashcardList
